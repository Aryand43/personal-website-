# Personal Website

Built to be clean, fast, and static.

## Status
Temporarily down.

## Local

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Static build

```bash
npm run build
```

Static output is generated to `out/`.
